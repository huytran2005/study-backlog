package com.example.brainnote.feature.splash

/**
 * SplashUiState represents the UI state model for the Splash Screen.
 */
data class SplashUiState(
    val appTitle: String = "Study Backlog",
    val isLoading: Boolean = true
)
